//Linkowoanko

//1.//td><NavLink to={'/table/' + table.id}><Button variant="primary">Show more</Button></NavLink></td>

//najpierw tworzony jes NavLink, ktory przekierwouje, na podstawie id = (wygenerowal 4 soliki w naszym przypadku)

//2. oczywsice jest do tego podlaczony Route:

//<Route path="/table/:id" element={<Table />} />
